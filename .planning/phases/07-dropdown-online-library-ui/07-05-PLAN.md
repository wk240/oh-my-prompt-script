---
phase: 07-dropdown-online-library-ui
plan: 05
type: execute
wave: 5
depends_on:
  - 07-02
files_modified:
  - src/content/components/PromptPreviewModal.tsx
  - src/content/components/DropdownContainer.tsx
autonomous: true
requirements:
  - NET-02
must_haves:
  truths:
    - "Modal opens when clicking NetworkPromptCard"
    - "Modal shows full prompt content"
    - "Modal shows source info (sourceProvider / sourceCategory)"
    - "Modal closes on Escape key press"
    - "Modal closes on overlay click"
    - "Modal has placeholder '收藏' button (disabled)"
  artifacts:
    - path: src/content/components/PromptPreviewModal.tsx
      provides: "Modal overlay for full prompt preview"
      exports: "PromptPreviewModal"
      min_lines: 80
    - path: src/content/components/DropdownContainer.tsx
      provides: "Modal state, Portal integration"
      contains: "PromptPreviewModal"
  key_links:
    - from: DropdownContainer.tsx
      to: PromptPreviewModal.tsx
      via: "import + render with selectedNetworkPrompt"
      pattern: "PromptPreviewModal"
    - from: NetworkPromptCard onClick
      to: setSelectedNetworkPrompt + setIsModalOpen
      via: "card click handler"
---

<objective>
Create PromptPreviewModal component for full prompt content display. Integrate with NetworkPromptCard click handler and implement close behaviors (Escape key, overlay click).

Purpose: Enable user to view full network prompt content with source attribution.
Output: PromptPreviewModal.tsx and modal integration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-dropdown-online-library-ui/07-CONTEXT.md
@.planning/phases/07-dropdown-online-library-ui/07-RESEARCH.md
@.planning/phases/07-dropdown-online-library-ui/07-UI-SPEC.md
@.planning/phases/07-dropdown-online-library-ui/07-01-SUMMARY.md
@.planning/phases/07-dropdown-online-library-ui/07-02-SUMMARY.md
@.planning/phases/07-dropdown-online-library-ui/07-03-SUMMARY.md
@.planning/phases/07-dropdown-online-library-ui/07-04-SUMMARY.md

<interfaces>
<!-- Key patterns executor needs -->

From src/shared/types.ts:
```typescript
export interface NetworkPrompt extends Prompt {
  sourceProvider?: string
  sourceCategory?: string
  previewImage?: string
  sourceUrl?: string
}
```

From UI-SPEC.md Visual Specifications:
```
PromptPreviewModal:
- Position: Fixed, centered (top: 50%, left: 50%, transform: translate(-50%, -50%))
- Dimensions: width: 400px, max-height: 480px
- Background: #ffffff
- Border-radius: 12px
- Shadow: box-shadow: 0 12px 32px rgba(0,0,0,0.2)
- Overlay: background: rgba(0,0,0,0.3)
- Animation: opacity fade-in 150ms ease-out
- Header: Title: 14px weight 600, Close button: 24x24px, right-aligned
- Content: 12px body, max-height: 320px overflow-y: auto, padding: 16px
- Source info: 10px, color #64748B, "来源: {provider} / {category}"
- Footer: Reserved for "收藏" button (Phase 8), height: 48px, padding: 12px
- Close behavior: Click overlay, click close button, press Escape
```

From src/content/components/DropdownContainer.tsx:
```typescript
const PORTAL_ID = 'prompt-script-dropdown-portal'
function getPortalContainer(): HTMLElement { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create PromptPreviewModal component</name>
  <files>src/content/components/PromptPreviewModal.tsx</files>
  <read_first>
    - src/shared/types.ts (NetworkPrompt interface)
    - src/content/components/DropdownContainer.tsx (PORTAL_ID, getPortalContainer pattern)
    - .planning/phases/07-dropdown-online-library-ui/07-UI-SPEC.md (Modal visual specs)
    - src/content/components/DropdownContainer.tsx (lucide-react imports pattern)
  </read_first>
  <behavior>
    - Test: Modal renders to Portal container
    - Test: Modal shows prompt.name as title
    - Test: Modal shows prompt.content in scrollable area
    - Test: Modal shows source info "来源: provider / category"
    - Test: Escape key triggers onClose
    - Test: Overlay click triggers onClose
    - Test: Close button triggers onClose
    - Test: "收藏" button is disabled placeholder
  </behavior>
  <action>
    Create new file `src/content/components/PromptPreviewModal.tsx`:

    ```typescript
    /**
     * PromptPreviewModal - Modal overlay for full prompt content display
     * Phase 7: Portal-rendered modal with escape/overlay close (D-07, D-08, D-09)
     */

    import { createPortal } from 'react-dom'
    import { useEffect, useCallback } from 'react'
    import { X, Bookmark } from 'lucide-react'
    import type { NetworkPrompt } from '../../shared/types'

    interface PromptPreviewModalProps {
      prompt: NetworkPrompt
      isOpen: boolean
      onClose: () => void
    }

    const PORTAL_ID = 'prompt-script-dropdown-portal'

    function getPortalContainer(): HTMLElement {
      let container = document.getElementById(PORTAL_ID)
      if (!container) {
        container = document.createElement('div')
        container.id = PORTAL_ID
        document.body.appendChild(container)
      }
      return container
    }

    export function PromptPreviewModal({ prompt, isOpen, onClose }: PromptPreviewModalProps) {
      // D-08: Escape key closes modal
      useEffect(() => {
        if (!isOpen) return
        const handleEscape = (e: KeyboardEvent) => {
          if (e.key === 'Escape') onClose()
        }
        document.addEventListener('keydown', handleEscape)
        return () => document.removeEventListener('keydown', handleEscape)
      }, [isOpen, onClose])

      // D-08: Click overlay closes modal
      const handleOverlayClick = useCallback((e: React.MouseEvent) => {
        if (e.target === e.currentTarget) onClose()
      }, [onClose])

      if (!isOpen) return null

      // Render to same Portal container as dropdown
      return createPortal(
        <>
          {/* Overlay */}
          <div
            className="modal-overlay"
            onClick={handleOverlayClick}
            style={{
              position: 'fixed',
              inset: 0,
              background: 'rgba(0,0,0,0.3)',
              zIndex: 2147483647,
            }}
          />
          {/* Modal */}
          <div
            className="modal-content"
            style={{
              position: 'fixed',
              top: '50%',
              left: '50%',
              transform: 'translate(-50%, -50%)',
              width: '400px',
              maxHeight: '480px',
              background: '#ffffff',
              borderRadius: '12px',
              boxShadow: '0 12px 32px rgba(0,0,0,0.2)',
              zIndex: 2147483647,
              display: 'flex',
              flexDirection: 'column',
            }}
          >
            {/* Header */}
            <div style={{
              padding: '16px',
              borderBottom: '1px solid #E5E5E5',
              display: 'flex',
              justifyContent: 'space-between',
              alignItems: 'center',
            }}>
              <span style={{ fontSize: '14px', fontWeight: 600, color: '#171717' }}>
                {prompt.name}
              </span>
              <button
                onClick={onClose}
                aria-label="关闭"
                style={{
                  width: '24px',
                  height: '24px',
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                  background: 'transparent',
                  border: 'none',
                  cursor: 'pointer',
                  color: '#171717',
                }}
              >
                <X style={{ width: 16, height: 16 }} />
              </button>
            </div>
            {/* Content */}
            <div style={{
              padding: '16px',
              maxHeight: '320px',
              overflow: 'auto',
              fontSize: '12px',
              color: '#171717',
              lineHeight: '1.4',
            }}>
              {prompt.content}
            </div>
            {/* Footer */}
            <div style={{
              padding: '12px 16px',
              borderTop: '1px solid #E5E5E5',
              display: 'flex',
              flexDirection: 'column',
              gap: '8px',
            }}>
              {/* Source info */}
              <div style={{ fontSize: '10px', color: '#64748B' }}>
                来源: {prompt.sourceProvider || 'Unknown'} / {prompt.sourceCategory || 'Unknown'}
              </div>
              {/* D-09: Placeholder "收藏" button for Phase 8 */}
              <button
                disabled
                style={{
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                  gap: '4px',
                  padding: '8px 16px',
                  background: '#f8f8f8',
                  border: '1px solid #E5E5E5',
                  borderRadius: '6px',
                  fontSize: '12px',
                  fontWeight: 500,
                  color: '#171717',
                  opacity: 0.5,
                  cursor: 'not-allowed',
                }}
              >
                <Bookmark style={{ width: 14, height: 14 }} />
                收藏
              </button>
            </div>
          </div>
        </>,
        getPortalContainer()
      )
    }
    ```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "error" | xargs -I {} test {} -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - File src/content/components/PromptPreviewModal.tsx exists
    - File contains `export function PromptPreviewModal`
    - File imports createPortal from react-dom
    - File imports X and Bookmark from lucide-react
    - Modal uses createPortal to PORTAL_ID container
    - Escape key handler calls onClose
    - Overlay click handler calls onClose when e.target === e.currentTarget
    - Modal shows prompt.name in header
    - Modal shows prompt.content in scrollable div
    - Modal shows source info with sourceProvider/sourceCategory
    - "收藏" button is disabled with opacity 0.5
    - TypeScript compilation passes with 0 errors
  </acceptance_criteria>
  <done>
    PromptPreviewModal component created with Portal rendering, Escape/overlay close, and placeholder "收藏" button.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add modal state and integrate with NetworkPromptCard</name>
  <files>src/content/components/DropdownContainer.tsx</files>
  <read_first>
    - src/content/components/DropdownContainer.tsx (state section from 07-01)
    - src/content/components/DropdownContainer.tsx (NetworkPromptCard onClick from 07-02)
    - src/content/components/PromptPreviewModal.tsx (component from Task 1)
  </read_first>
  <behavior>
    - Test: isModalOpen state initializes to false
    - Test: selectedNetworkPrompt state initializes to null
    - Test: Clicking NetworkPromptCard sets modal state and opens modal
    - Test: Modal onClose resets modal state
  </behavior>
  <action>
    Import PromptPreviewModal at top of DropdownContainer.tsx:
    ```typescript
    import { PromptPreviewModal } from './PromptPreviewModal'
    ```

    Add modal state variables (add to state section from 07-01, after loadedCount):

    ```typescript
    // Phase 7: Modal state for prompt preview (D-07)
    const [isModalOpen, setIsModalOpen] = useState(false)
    const [selectedNetworkPrompt, setSelectedNetworkPrompt] = useState<NetworkPrompt | null>(null)
    ```

    Update NetworkPromptCard onClick handler (from 07-02) to open modal:

    ```typescript
    onClick={() => {
      setSelectedNetworkPrompt(prompt)
      setIsModalOpen(true)
    }}
    ```

    Replace the placeholder console.log with actual modal trigger.

    Add PromptPreviewModal render at end of dropdown-container (after dropdown-main div, before createPortal closing):

    ```typescript
    {/* Phase 7: Prompt preview modal (D-07) */}
    {selectedNetworkPrompt && (
      <PromptPreviewModal
        prompt={selectedNetworkPrompt}
        isOpen={isModalOpen}
        onClose={() => {
          setIsModalOpen(false)
          setSelectedNetworkPrompt(null)
        }}
      />
    )}
    ```

    This should be placed inside the createPortal call, as a sibling to the dropdown-container div.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "error" | xargs -I {} test {} -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - DropdownContainer.tsx imports PromptPreviewModal from './PromptPreviewModal'
    - DropdownContainer.tsx contains `const [isModalOpen, setIsModalOpen] = useState(false)`
    - DropdownContainer.tsx contains `const [selectedNetworkPrompt, setSelectedNetworkPrompt] = useState<NetworkPrompt | null>(null)`
    - NetworkPromptCard onClick sets selectedNetworkPrompt and setIsModalOpen(true)
    - PromptPreviewModal rendered with selectedNetworkPrompt, isModalOpen, onClose props
    - onClose resets both isModalOpen and selectedNetworkPrompt to false/null
    - TypeScript compilation passes with 0 errors
  </acceptance_criteria>
  <done>
    Modal state added, NetworkPromptCard click triggers modal open, onClose resets state.
  </done>
</task>

</tasks>

<verification>
**Manual validation after build:**
1. Run `npm run dev`
2. Load extension in Chrome from `dist/` folder
3. Open Lovart page, trigger dropdown
4. Click "在线库", click any NetworkPromptCard
5. Verify Modal opens with full prompt content
6. Verify source info displayed correctly
7. Press Escape, verify Modal closes
8. Click outside Modal (overlay), verify Modal closes
9. Verify "收藏" button is disabled (placeholder)
</verification>

<success_criteria>
- Modal opens on NetworkPromptCard click (D-07)
- Modal shows full prompt content + source info
- Escape key closes modal (D-08)
- Overlay click closes modal (D-08)
- "收藏" button placeholder disabled (D-09)
- TypeScript compiles with 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-dropdown-online-library-ui/07-05-SUMMARY.md`
</output>