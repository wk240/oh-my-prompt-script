---
phase: 10-api-key-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/shared/constants.ts, src/shared/messages.ts, src/shared/types.ts]
autonomous: true
requirements: [AUTH-02, AUTH-04]
user_setup: []
must_haves:
  truths:
    - "VisionApiConfig interface exists with baseUrl, apiKey, modelName fields"
    - "VISION_API_CONFIG_STORAGE_KEY constant exists with value '_visionApiConfig'"
    - "MessageType has GET_API_CONFIG, SET_API_CONFIG, DELETE_API_CONFIG entries"
  artifacts:
    - path: "src/shared/constants.ts"
      provides: "Storage key constant for API config"
      contains: "VISION_API_CONFIG_STORAGE_KEY"
    - path: "src/shared/messages.ts"
      provides: "Message types for API config operations"
      contains: "GET_API_CONFIG"
    - path: "src/shared/types.ts"
      provides: "VisionApiConfig interface"
      contains: "interface VisionApiConfig"
  key_links:
    - from: "src/shared/constants.ts"
      to: "chrome.storage.local"
      via: "VISION_API_CONFIG_STORAGE_KEY"
      pattern: "VISION_API_CONFIG_STORAGE_KEY = '_visionApiConfig'"
---

<objective>
Define the foundational types, constants, and message types for API configuration storage.

Purpose: Establish contracts for Vision AI API configuration that downstream tasks will implement against (per D-03, D-05).
Output: VisionApiConfig interface, storage key constant, MessageType extensions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-api-key-management/10-CONTEXT.md
@.planning/phases/10-api-key-management/10-RESEARCH.md
@.planning/phases/10-api-key-management/10-UI-SPEC.md

<interfaces>
<!-- Existing patterns executor must follow. No exploration needed. -->

From src/shared/constants.ts (line 34):
```typescript
export const CAPTURED_IMAGE_STORAGE_KEY = '_capturedImageUrl'
```
Pattern: Underscore prefix for separate storage keys, follows Phase 9 pattern per D-03.

From src/shared/messages.ts (lines 1-25):
```typescript
export enum MessageType {
  PING = 'PING',
  GET_STORAGE = 'GET_STORAGE',
  // ... existing types ...
  SET_SETTINGS_ONLY = 'SET_SETTINGS_ONLY'
}
```
Pattern: UPPERCASE enum values, add new types at end before closing brace.

From src/shared/types.ts:
```typescript
export interface StorageSchema {
  version: string
  userData: UserData
  settings: SyncSettings
  _migrationComplete?: boolean
}
```
Pattern: Interface with typed fields, VisionApiConfig is separate (not in StorageSchema per D-03).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add VisionApiConfig interface to types.ts</name>
  <files>src/shared/types.ts</files>
  <read_first>
    - src/shared/types.ts — see existing interface patterns
  </read_first>
  <behavior>
    - VisionApiConfig interface has: baseUrl (string), apiKey (string), modelName (string)
    - Optional configuredAt (number) timestamp field
  </behavior>
  <action>Add VisionApiConfig interface after existing interfaces (per D-05). Fields: baseUrl: string, apiKey: string, modelName: string, configuredAt?: number. Do NOT add to StorageSchema — separate storage key pattern per D-03.</action>
  <verify>
    <automated>grep -n "interface VisionApiConfig" src/shared/types.ts</automated>
    <manual>Verify fields: baseUrl, apiKey, modelName, configuredAt?</manual>
  </verify>
  <done>VisionApiConfig interface exported from types.ts with all four fields defined.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add VISION_API_CONFIG_STORAGE_KEY to constants.ts</name>
  <files>src/shared/constants.ts</files>
  <read_first>
    - src/shared/constants.ts — see CAPTURED_IMAGE_STORAGE_KEY pattern (line 34)
  </read_first>
  <behavior>
    - Constant exists with value '_visionApiConfig'
    - Underscore prefix matches Phase 9 pattern
  </behavior>
  <action>Add VISION_API_CONFIG_STORAGE_KEY constant with value '_visionApiConfig' after CAPTURED_IMAGE_STORAGE_KEY (per D-03). Follow same naming pattern: underscore prefix, descriptive name.</action>
  <verify>
    <automated>grep -n "VISION_API_CONFIG_STORAGE_KEY = '_visionApiConfig'" src/shared/constants.ts</automated>
  </verify>
  <done>VISION_API_CONFIG_STORAGE_KEY exported from constants.ts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add API config MessageType entries</name>
  <files>src/shared/messages.ts</files>
  <read_first>
    - src/shared/messages.ts — see enum structure and existing types
  </read_first>
  <behavior>
    - Three new enum entries: GET_API_CONFIG, SET_API_CONFIG, DELETE_API_CONFIG
    - Entries appear before closing brace
  </behavior>
  <action>Add three MessageType enum entries at end (before closing brace): GET_API_CONFIG = 'GET_API_CONFIG', SET_API_CONFIG = 'SET_API_CONFIG', DELETE_API_CONFIG = 'DELETE_API_CONFIG'. Maintain alphabetical-ish ordering within logical groupings.</action>
  <verify>
    <automated>grep -n "GET_API_CONFIG\|SET_API_CONFIG\|DELETE_API_CONFIG" src/shared/messages.ts | wc -l</automated>
    <manual>Expected: 3 lines (one per enum entry)</manual>
  </verify>
  <done>All three API config MessageType entries present in messages.ts enum.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Type definitions → Runtime | Types guide implementation, no runtime security impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-t | Information Disclosure | VisionApiConfig.apiKey field | accept | Interface definition — runtime storage handles protection |
</threat_model>

<verification>
All three files modified:
- types.ts: VisionApiConfig interface exists
- constants.ts: VISION_API_CONFIG_STORAGE_KEY exists
- messages.ts: 3 new MessageType entries exist
</verification>

<success_criteria>
- grep finds VisionApiConfig in types.ts
- grep finds VISION_API_CONFIG_STORAGE_KEY in constants.ts
- grep finds all 3 new MessageType entries in messages.ts
</success_criteria>

<output>
After completion, create `.planning/phases/10-api-key-management/10-01-SUMMARY.md`
</output>