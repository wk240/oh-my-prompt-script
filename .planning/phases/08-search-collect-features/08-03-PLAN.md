---
phase: 08-search-collect-features
plan: 03
type: execute
wave: 2
depends_on: [08-02]
files_modified: [src/content/components/PromptPreviewModal.tsx]
autonomous: true
requirements: [NET-03]
user_setup: []

must_haves:
  truths:
    - "User can click '收藏' button in Modal footer"
    - "Button is no longer disabled"
    - "Click triggers onCollect callback"
  artifacts:
    - path: "src/content/components/PromptPreviewModal.tsx"
      provides: "Active collect button"
      contains: "onClick={onCollect}, disabled removed"
  key_links:
    - from: "PromptPreviewModal.tsx"
      to: "DropdownContainer.tsx"
      via: "onCollect prop"
      pattern: "onCollect.*NetworkPrompt"
---

<objective>
Activate the existing "收藏" button in PromptPreviewModal (D-09, D-10).

Purpose: Enable users to collect prompts from preview modal
Output: Active button with onClick handler, onCollect prop added
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-search-collect-features/08-CONTEXT.md
@.planning/phases/08-search-collect-features/08-RESEARCH.md

<interfaces>
<!-- Current PromptPreviewModal interface and button -->

From src/content/components/PromptPreviewModal.tsx:
```typescript
interface PromptPreviewModalProps {
  prompt: NetworkPrompt
  isOpen: boolean
  onClose: () => void
}

// Current disabled button (Phase 7 placeholder):
<button
  disabled
  style={{
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    gap: '4px',
    padding: '8px 16px',
    background: '#f8f8f8',
    border: '1px solid #E5E5E5',
    borderRadius: '6px',
    fontSize: '12px',
    fontWeight: 500,
    color: '#171717',
    opacity: 0.5,
    cursor: 'not-allowed',
  }}
>
  <Bookmark style={{ width: 14, height: 14 }} />
  收藏
</button>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add onCollect prop to PromptPreviewModal interface</name>
  <files>src/content/components/PromptPreviewModal.tsx</files>
  <behavior>
    - Test 1: PromptPreviewModalProps contains onCollect?: (prompt: NetworkPrompt) => void
    - Test 2: Component accepts onCollect prop with default undefined
  </behavior>
  <read_first>
    - src/content/components/PromptPreviewModal.tsx (current interface around line 11)
    - src/shared/types.ts (NetworkPrompt type)
  </read_first>
  <action>
    Modify PromptPreviewModal.tsx interface:

    1. Extend PromptPreviewModalProps with onCollect:
       ```typescript
       interface PromptPreviewModalProps {
         prompt: NetworkPrompt
         isOpen: boolean
         onClose: () => void
         // Phase 8: Collect callback (D-09)
         onCollect?: (prompt: NetworkPrompt) => void
       }
       ```

    2. Update function signature:
       ```typescript
       export function PromptPreviewModal({
         prompt,
         isOpen,
         onClose,
         onCollect
       }: PromptPreviewModalProps)
       ```
  </action>
  <verify>
    <automated>grep -n "onCollect" src/content/components/PromptPreviewModal.tsx | head -5</automated>
  </verify>
  <acceptance_criteria>
    - PromptPreviewModalProps interface contains `onCollect?: (prompt: NetworkPrompt) => void`
    - Function signature includes `onCollect` parameter
  </acceptance_criteria>
  <done>
    onCollect prop added to PromptPreviewModal interface.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Activate "收藏" button and add onClick handler</name>
  <files>src/content/components/PromptPreviewModal.tsx</files>
  <behavior>
    - Test 1: Button no longer has disabled attribute
    - Test 2: Button onClick calls onCollect(prompt) if onCollect exists
    - Test 3: Button cursor is 'pointer' (not 'not-allowed')
    - Test 4: Button opacity is 1 (not 0.5)
    - Test 5: Button background is '#1890ff' (accent color per UI-SPEC)
    - Test 6: Button text color is '#fff'
  </behavior>
  <read_first>
    - src/content/components/PromptPreviewModal.tsx (current disabled button around line 130)
    - .planning/phases/08-search-collect-features/08-UI-SPEC.md (button styles: #1890ff accent)
  </read_first>
  <action>
    Modify PromptPreviewModal.tsx button in footer section (around line 130):

    Replace the disabled placeholder button:
    ```typescript
    {/* D-09: Placeholder "收藏" button for Phase 8 */}
    <button
      disabled
      style={{
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        gap: '4px',
        padding: '8px 16px',
        background: '#f8f8f8',
        border: '1px solid #E5E5E5',
        borderRadius: '6px',
        fontSize: '12px',
        fontWeight: 500,
        color: '#171717',
        opacity: 0.5,
        cursor: 'not-allowed',
      }}
    >
      <Bookmark style={{ width: 14, height: 14 }} />
      收藏
    </button>
    ```

    With active button:
    ```typescript
    {/* Phase 8: Active "收藏" button (D-09) */}
    <button
      onClick={() => onCollect?.(prompt)}
      aria-label="收藏提示词"
      style={{
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        gap: '4px',
        padding: '8px 16px',
        background: '#1890ff', // D-09: Accent color (UI-SPEC)
        border: 'none',
        borderRadius: '6px',
        fontSize: '12px',
        fontWeight: 500,
        color: '#fff',
        cursor: 'pointer',
      }}
    >
      <Bookmark style={{ width: 14, height: 14 }} />
      收藏
    </button>
    ```
  </action>
  <verify>
    <automated>grep -n "onClick.*onCollect" src/content/components/PromptPreviewModal.tsx && grep -n "background: '#1890ff'" src/content/components/PromptPreviewModal.tsx</automated>
  </verify>
  <acceptance_criteria>
    - PromptPreviewModal.tsx does NOT contain `disabled` attribute on the collect button
    - PromptPreviewModal.tsx contains `onClick={() => onCollect?.(prompt)}`
    - PromptPreviewModal.tsx contains `background: '#1890ff'`
    - PromptPreviewModal.tsx contains `color: '#fff'`
    - PromptPreviewModal.tsx contains `cursor: 'pointer'`
    - PromptPreviewModal.tsx does NOT contain `opacity: 0.5`
  </acceptance_criteria>
  <done>
    "收藏" button activated with onClick handler (D-09 complete).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Button click → Collect action | User-initiated collect triggers state changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Tampering | Collect button | accept | Button calls controlled callback, no injection risk |
</threat_model>

<verification>
- Manual: Open Modal on network prompt, verify "收藏" button is blue/active
- Manual: Click button, verify onCollect callback triggered (will show dialog in Plan 04)
</verification>

<success_criteria>
- Button no longer disabled (D-09)
- Button uses accent color #1890ff (UI-SPEC)
- onClick calls onCollect(prompt) (D-09)
</success_criteria>

<output>
After completion, create `.planning/phases/08-search-collect-features/08-03-SUMMARY.md`
</output>