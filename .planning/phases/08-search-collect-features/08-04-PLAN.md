---
phase: 08-search-collect-features
plan: 04
type: execute
wave: 2
depends_on: [08-03]
files_modified: [src/content/components/CategorySelectDialog.tsx, src/content/components/DropdownContainer.tsx]
autonomous: true
requirements: [NET-03]
user_setup: []

must_haves:
  truths:
    - "User sees dialog with existing local categories"
    - "User can select a category by clicking"
    - "Selected category shows checkmark icon"
    - "User can type new category name in input"
    - "User can click '确认收藏' or '取消'"
  artifacts:
    - path: "src/content/components/CategorySelectDialog.tsx"
      provides: "Portal-rendered category selector dialog"
      exports: ["CategorySelectDialog"]
      min_lines: 100
  key_links:
    - from: "DropdownContainer.tsx"
      to: "CategorySelectDialog"
      via: "onConfirm callback"
      pattern: "CategorySelectDialog.*isOpen"
---

<objective>
Create CategorySelectDialog component (D-11 through D-15).

Purpose: Allow users to select target category when collecting prompts
Output: Portal-rendered inline styled dialog with category list + new category input
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-search-collect-features/08-CONTEXT.md
@.planning/phases/08-search-collect-features/08-RESEARCH.md
@.planning/phases/08-search-collect-features/08-UI-SPEC.md

<interfaces>
<!-- Pattern from PromptPreviewModal for Portal rendering -->

From src/content/components/PromptPreviewModal.tsx:
```typescript
import { createPortal } from 'react-dom'
import { useEffect, useCallback } from 'react'
import { X, Bookmark } from 'lucide-react'

const PORTAL_ID = 'prompt-script-dropdown-portal'

function getPortalContainer(): HTMLElement {
  let container = document.getElementById(PORTAL_ID)
  if (!container) {
    container = document.createElement('div')
    container.id = PORTAL_ID
    document.body.appendChild(container)
  }
  return container
}

// Overlay style:
position: 'fixed',
inset: 0,
background: 'rgba(0,0,0,0.3)',
zIndex: 2147483647,

// Modal style:
position: 'fixed',
top: '50%',
left: '50%',
transform: 'translate(-50%, -50%)',
background: '#ffffff',
borderRadius: '12px',
boxShadow: '0 12px 32px rgba(0,0,0,0.2)',
zIndex: 2147483647,
```

From src/shared/types.ts:
```typescript
export interface Category {
  id: string
  name: string
  order: number
}
```

From src/content/components/DropdownContainer.tsx:
```typescript
const [localCategories, setLocalCategories] = useState<Category[]>([])
// categories computed value includes localCategories
```

From UI-SPEC.md:
```typescript
// Dialog dimensions:
width: 320px
maxHeight: 400px
header padding: 16px
header border-bottom: 1px solid #E5E5E5
list max-height: 200px
list overflow: auto
category item padding: 12px 12px
category item border-radius: 6px
selected background: #f0f0f0
check icon: 14x14px, Lucide Check
input section border-top: 1px solid #E5E5E5
input padding: 8px 12px
input border: 1px solid #E5E5E5
input border-radius: 6px
footer padding: 12px 16px
footer border-top: 1px solid #E5E5E5
cancel button: flex: 1, background: #f8f8f8, border-radius: 6px
confirm button: flex: 1, background: #1890ff, color: #fff, border-radius: 6px
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create CategorySelectDialog component file</name>
  <files>src/content/components/CategorySelectDialog.tsx</files>
  <behavior>
    - Test 1: Component file exists
    - Test 2: Component exports CategorySelectDialog function
    - Test 3: Component uses Portal pattern (createPortal)
    - Test 4: Component renders overlay + dialog container
  </behavior>
  <read_first>
    - src/content/components/PromptPreviewModal.tsx (Portal pattern reference)
    - src/shared/types.ts (Category type)
    - .planning/phases/08-search-collect-features/08-UI-SPEC.md (exact dimensions)
  </read_first>
  <action>
    Create src/content/components/CategorySelectDialog.tsx:

    ```typescript
    /**
     * CategorySelectDialog - Portal-rendered dialog for selecting target category
     * Phase 8: Inline styled dialog for collect flow (D-11 through D-15)
     */

    import { createPortal } from 'react-dom'
    import { useState, useEffect, useCallback } from 'react'
    import { Check, Plus, X } from 'lucide-react'
    import type { Category } from '../../shared/types'

    const PORTAL_ID = 'prompt-script-dropdown-portal'

    function getPortalContainer(): HTMLElement {
      let container = document.getElementById(PORTAL_ID)
      if (!container) {
        container = document.createElement('div')
        container.id = PORTAL_ID
        document.body.appendChild(container)
      }
      return container
    }

    interface CategorySelectDialogProps {
      categories: Category[]
      isOpen: boolean
      onClose: () => void
      onConfirm: (categoryId: string, newCategoryName?: string) => void
    }

    export function CategorySelectDialog({
      categories,
      isOpen,
      onClose,
      onConfirm
    }: CategorySelectDialogProps) {
      const [selectedCategoryId, setSelectedCategoryId] = useState<string | null>(null)
      const [newCategoryName, setNewCategoryName] = useState('')
      const [isCreatingNew, setIsCreatingNew] = useState(false)

      // Reset state when dialog opens
      useEffect(() => {
        if (isOpen) {
          setSelectedCategoryId(null)
          setNewCategoryName('')
          setIsCreatingNew(false)
        }
      }, [isOpen])

      // Escape key handler
      useEffect(() => {
        if (!isOpen) return
        const handleEscape = (e: KeyboardEvent) => {
          if (e.key === 'Escape') onClose()
        }
        document.addEventListener('keydown', handleEscape)
        return () => document.removeEventListener('keydown', handleEscape)
      }, [isOpen, onClose])

      // Overlay click handler
      const handleOverlayClick = useCallback((e: React.MouseEvent) => {
        if (e.target === e.currentTarget) onClose()
      }, [onClose])

      // Handle category selection
      const handleCategorySelect = (categoryId: string) => {
        setSelectedCategoryId(categoryId)
        setIsCreatingNew(false)
        setNewCategoryName('')
      }

      // Handle new category input
      const handleNewCategoryChange = (value: string) => {
        setNewCategoryName(value)
        setIsCreatingNew(value.trim().length > 0)
        if (value.trim().length > 0) {
          setSelectedCategoryId(null)
        }
      }

      // Handle confirm
      const handleConfirm = () => {
        if (isCreatingNew && newCategoryName.trim()) {
          onConfirm('', newCategoryName.trim())
        } else if (selectedCategoryId) {
          onConfirm(selectedCategoryId)
        }
      }

      // Check if confirm is valid
      const canConfirm = selectedCategoryId !== null || (isCreatingNew && newCategoryName.trim().length > 0)

      if (!isOpen) return null

      return createPortal(
        <>
          {/* Overlay */}
          <div
            onClick={handleOverlayClick}
            style={{
              position: 'fixed',
              inset: 0,
              background: 'rgba(0,0,0,0.3)',
              zIndex: 2147483647,
            }}
          />
          {/* Dialog container (D-11: Portal, D-12: Category list + new category) */}
          <div
            style={{
              position: 'fixed',
              top: '50%',
              left: '50%',
              transform: 'translate(-50%, -50%)',
              width: '320px', // UI-SPEC
              maxHeight: '400px', // UI-SPEC
              background: '#ffffff',
              borderRadius: '12px',
              boxShadow: '0 12px 32px rgba(0,0,0,0.2)',
              zIndex: 2147483647,
              display: 'flex',
              flexDirection: 'column',
            }}
          >
            {/* Header (D-15) */}
            <div style={{
              padding: '16px', // UI-SPEC
              borderBottom: '1px solid #E5E5E5', // UI-SPEC
            }}>
              <span style={{ fontSize: '14px', fontWeight: 600, color: '#171717' }}>
                选择收藏分类
              </span>
            </div>

            {/* Category list (D-12, D-13) */}
            <div style={{
              padding: '12px',
              maxHeight: '200px', // UI-SPEC
              overflow: 'auto', // UI-SPEC
            }}>
              {categories.map(cat => (
                <div
                  key={cat.id}
                  onClick={() => handleCategorySelect(cat.id)}
                  role="option"
                  aria-selected={selectedCategoryId === cat.id}
                  style={{
                    padding: '12px', // UI-SPEC
                    display: 'flex',
                    alignItems: 'center',
                    background: selectedCategoryId === cat.id ? '#f0f0f0' : 'transparent', // UI-SPEC
                    borderRadius: '6px', // UI-SPEC
                    cursor: 'pointer',
                    marginBottom: '4px',
                  }}
                >
                  {selectedCategoryId === cat.id && (
                    <Check style={{ width: 14, height: 14, marginRight: 8, color: '#171717' }} /> // UI-SPEC
                  )}
                  <span style={{ fontSize: '12px', color: '#171717' }}>{cat.name}</span>
                </div>
              ))}
            </div>

            {/* New category input section (D-14) */}
            <div style={{
              padding: '12px',
              borderTop: '1px solid #E5E5E5', // UI-SPEC
            }}>
              <div style={{ display: 'flex', gap: '8px' }}>
                <input
                  value={newCategoryName}
                  onChange={(e) => handleNewCategoryChange(e.target.value)}
                  placeholder="新建分类..."
                  aria-label="新建分类"
                  style={{
                    flex: 1,
                    padding: '8px 12px', // UI-SPEC
                    border: '1px solid #E5E5E5', // UI-SPEC
                    borderRadius: '6px', // UI-SPEC
                    fontSize: '12px',
                    outline: 'none',
                  }}
                />
                <button
                  onClick={() => {
                    if (newCategoryName.trim()) {
                      handleNewCategoryChange(newCategoryName)
                      setIsCreatingNew(true)
                    }
                  }}
                  disabled={!newCategoryName.trim()}
                  aria-label="创建"
                  style={{
                    padding: '8px 12px',
                    background: '#f8f8f8', // UI-SPEC
                    border: '1px solid #E5E5E5',
                    borderRadius: '6px',
                    fontSize: '12px',
                    cursor: newCategoryName.trim() ? 'pointer' : 'not-allowed',
                    opacity: newCategoryName.trim() ? 1 : 0.5,
                    display: 'flex',
                    alignItems: 'center',
                    justifyContent: 'center',
                  }}
                >
                  <Plus style={{ width: 14, height: 14 }} />
                </button>
              </div>
            </div>

            {/* Footer (D-15) */}
            <div style={{
              padding: '12px 16px', // UI-SPEC
              borderTop: '1px solid #E5E5E5', // UI-SPEC
              display: 'flex',
              gap: '8px',
            }}>
              <button
                onClick={onClose}
                aria-label="取消"
                style={{
                  flex: 1, // UI-SPEC
                  padding: '10px',
                  background: '#f8f8f8', // UI-SPEC
                  border: '1px solid #E5E5E5',
                  borderRadius: '6px', // UI-SPEC
                  fontSize: '12px',
                  fontWeight: 500,
                  color: '#171717',
                  cursor: 'pointer',
                }}
              >
                取消
              </button>
              <button
                onClick={handleConfirm}
                disabled={!canConfirm}
                aria-label="确认收藏"
                style={{
                  flex: 1, // UI-SPEC
                  padding: '10px',
                  background: '#1890ff', // UI-SPEC accent
                  border: 'none',
                  borderRadius: '6px', // UI-SPEC
                  fontSize: '12px',
                  fontWeight: 500,
                  color: '#fff',
                  cursor: canConfirm ? 'pointer' : 'not-allowed',
                  opacity: canConfirm ? 1 : 0.5,
                }}
              >
                确认收藏
              </button>
            </div>
          </div>
        </>,
        getPortalContainer()
      )
    }
    ```
  </action>
  <verify>
    <automated>ls src/content/components/CategorySelectDialog.tsx && grep -n "export function CategorySelectDialog" src/content/components/CategorySelectDialog.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File src/content/components/CategorySelectDialog.tsx exists
    - File contains `export function CategorySelectDialog`
    - File contains `createPortal`
    - File contains `PORTAL_ID = 'prompt-script-dropdown-portal'`
    - File contains `width: '320px'`
    - File contains `maxHeight: '200px'` for list
    - File contains `选择收藏分类`
    - File contains `确认收藏`
    - File contains `取消`
    - File contains `新建分类...`
  </acceptance_criteria>
  <done>
    CategorySelectDialog component created with Portal pattern (D-11 through D-15).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Import CategorySelectDialog in DropdownContainer</name>
  <files>src/content/components/DropdownContainer.tsx</files>
  <behavior>
    - Test 1: DropdownContainer imports CategorySelectDialog
    - Test 2: CategorySelectDialog imported from './CategorySelectDialog'
  </behavior>
  <read_first>
    - src/content/components/DropdownContainer.tsx (import section around line 1)
  </read_first>
  <action>
    Add import for CategorySelectDialog in DropdownContainer.tsx imports section:

    ```typescript
    import { CategorySelectDialog } from './CategorySelectDialog'
    ```

    Add to the existing imports block (around line 17-21).
  </action>
  <verify>
    <automated>grep -n "import.*CategorySelectDialog" src/content/components/DropdownContainer.tsx</automated>
  </verify>
  <acceptance_criteria>
    - DropdownContainer.tsx contains `import { CategorySelectDialog } from './CategorySelectDialog'`
  </acceptance_criteria>
  <done>
    CategorySelectDialog imported in DropdownContainer.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add dialog state and pass onCollect to PromptPreviewModal</name>
  <files>src/content/components/DropdownContainer.tsx</files>
  <behavior>
    - Test 1: isCategoryDialogOpen state exists
    - Test 2: PromptPreviewModal receives onCollect prop
    - Test 3: onCollect opens CategorySelectDialog
    - Test 4: CategorySelectDialog receives categories from localCategories
  </behavior>
  <read_first>
    - src/content/components/DropdownContainer.tsx (state section around line 600, PromptPreviewModal usage around line 1053)
    - src/content/components/PromptPreviewModal.tsx (onCollect prop interface)
  </read_first>
  <action>
    Modify DropdownContainer.tsx:

    1. Add dialog state after isModalOpen state (around line 605):
       ```typescript
       // Phase 8: Category select dialog state (D-09)
       const [isCategoryDialogOpen, setIsCategoryDialogOpen] = useState(false)
       ```

    2. Add handleOpenCategoryDialog function:
       ```typescript
       // Phase 8: Open category dialog from Modal (D-09)
       const handleOpenCategoryDialog = useCallback(() => {
         setIsCategoryDialogOpen(true)
       }, [])
       ```

    3. Modify PromptPreviewModal usage (around line 1053) to pass onCollect:
       ```typescript
       {selectedNetworkPrompt && (
         <PromptPreviewModal
           prompt={selectedNetworkPrompt}
           isOpen={isModalOpen}
           onClose={() => {
             setIsModalOpen(false)
             setSelectedNetworkPrompt(null)
           }}
           onCollect={handleOpenCategoryDialog} // Phase 8: D-09
         />
       )}
       ```

    4. Add CategorySelectDialog rendering after PromptPreviewModal (in the Portal section):
       ```typescript
       {/* Phase 8: Category select dialog (D-11) */}
       <CategorySelectDialog
         categories={sortableCategories} // Exclude 'all' virtual category
         isOpen={isCategoryDialogOpen}
         onClose={() => setIsCategoryDialogOpen(false)}
         onConfirm={(categoryId, newCategoryName) => {
           // Placeholder for Plan 05 integration
           console.log('[Prompt-Script] Collect confirmed:', { categoryId, newCategoryName })
           setIsCategoryDialogOpen(false)
         }}
       />
       ```
  </action>
  <verify>
    <automated>grep -n "isCategoryDialogOpen" src/content/components/DropdownContainer.tsx | head -5 && grep -n "CategorySelectDialog" src/content/components/DropdownContainer.tsx | grep -v "import"</automated>
  </verify>
  <acceptance_criteria>
    - DropdownContainer.tsx contains `const [isCategoryDialogOpen, setIsCategoryDialogOpen] = useState(false)`
    - DropdownContainer.tsx contains `onCollect={handleOpenCategoryDialog}` in PromptPreviewModal
    - DropdownContainer.tsx contains `<CategorySelectDialog`
    - DropdownContainer.tsx contains `isOpen={isCategoryDialogOpen}` in CategorySelectDialog
    - DropdownContainer.tsx contains `categories={sortableCategories}` in CategorySelectDialog
  </acceptance_criteria>
  <done>
    CategorySelectDialog integrated with Modal collect flow (D-09, D-11 connected).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| New category input → Storage | User-created category name needs validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | newCategoryName input | mitigate | Trim input whitespace, validate non-empty before creating (ASVS V5) |
| T-08-06 | Spoofing | Category selection | accept | Selection is local state, no security impact |
</threat_model>

<verification>
- Manual: Click "收藏" in Modal, verify CategorySelectDialog appears
- Manual: Click existing category, verify checkmark appears
- Manual: Type new category name, verify input enabled
- Manual: Click "确认收藏", verify dialog closes
</verification>

<success_criteria>
- Dialog shows existing local categories (D-12)
- Radio select style with checkmark (D-13)
- New category input with "创建" button (D-14)
- Header "选择收藏分类", footer "取消" + "确认收藏" (D-15)
- Escape key and overlay click close dialog
</success_criteria>

<output>
After completion, create `.planning/phases/08-search-collect-features/08-04-SUMMARY.md`
</output>