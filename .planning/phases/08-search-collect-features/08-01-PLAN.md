---
phase: 08-search-collect-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/content/components/CacheStatusHeader.tsx]
autonomous: true
requirements: [NET-01]
user_setup: []

must_haves:
  truths:
    - "User can click search icon to expand input"
    - "Search input shows placeholder '搜索提示词...'"
    - "Input auto-focuses when expanded"
    - "User can click X icon to collapse and clear input"
  artifacts:
    - path: "src/content/components/CacheStatusHeader.tsx"
      provides: "Search icon + expandable input UI"
      contains: "Search, X icons from lucide-react"
  key_links:
    - from: "CacheStatusHeader.tsx"
      to: "DropdownContainer.tsx"
      via: "onSearchChange callback prop"
      pattern: "onSearchChange.*string"
---

<objective>
Implement search icon and expandable input in CacheStatusHeader (D-01 through D-04).

Purpose: Enable users to trigger search UI from the header
Output: Search icon button + 200px expandable input with close button
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-search-collect-features/08-CONTEXT.md
@.planning/phases/08-search-collect-features/08-RESEARCH.md
@.planning/phases/08-search-collect-features/08-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/shared/types.ts:
```typescript
// No new types needed for this plan
```

From src/content/components/CacheStatusHeader.tsx (existing interface):
```typescript
interface CacheStatusHeaderProps {
  fetchTimestamp?: string
  isExpired?: boolean
  isFromCache?: boolean
}
```

Existing imports in CacheStatusHeader.tsx:
```typescript
import { AlertCircle, WifiOff } from 'lucide-react'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add search UI props and imports to CacheStatusHeader</name>
  <files>src/content/components/CacheStatusHeader.tsx</files>
  <behavior>
    - Test 1: Component accepts isSearchExpanded, searchQuery, onSearchExpand, onSearchChange, onSearchClose props
    - Test 2: Component renders Search icon from lucide-react
    - Test 3: Component renders X icon when isSearchExpanded is true
  </behavior>
  <read_first>
    - src/content/components/CacheStatusHeader.tsx (current implementation)
    - src/content/components/DropdownContainer.tsx (header usage pattern)
  </read_first>
  <action>
    Modify CacheStatusHeader.tsx:

    1. Add imports for Search and X icons from lucide-react:
       ```typescript
       import { AlertCircle, WifiOff, Search, X } from 'lucide-react'
       ```

    2. Extend CacheStatusHeaderProps interface with search props:
       ```typescript
       interface CacheStatusHeaderProps {
         fetchTimestamp?: string
         isExpired?: boolean
         isFromCache?: boolean
         // Phase 8: Search UI props (D-01, D-04)
         isSearchExpanded?: boolean
         searchQuery?: string
         onSearchExpand?: () => void
         onSearchChange?: (query: string) => void
         onSearchClose?: () => void
       }
       ```

    3. Update function signature to accept new props:
       ```typescript
       export function CacheStatusHeader({
         fetchTimestamp,
         isExpired,
         isFromCache,
         isSearchExpanded = false,
         searchQuery = '',
         onSearchExpand,
         onSearchChange,
         onSearchClose
       }: CacheStatusHeaderProps)
       ```

    All props are optional with default values to maintain backward compatibility.
  </action>
  <verify>
    <automated>grep -n "Search, X" src/content/components/CacheStatusHeader.tsx && grep -n "isSearchExpanded" src/content/components/CacheStatusHeader.tsx</automated>
  </verify>
  <acceptance_criteria>
    - CacheStatusHeader.tsx contains `import { AlertCircle, WifiOff, Search, X } from 'lucide-react'`
    - CacheStatusHeaderProps interface contains `isSearchExpanded?: boolean`
    - CacheStatusHeaderProps interface contains `searchQuery?: string`
    - CacheStatusHeaderProps interface contains `onSearchExpand?: () => void`
    - CacheStatusHeaderProps interface contains `onSearchChange?: (query: string) => void`
    - CacheStatusHeaderProps interface contains `onSearchClose?: () => void`
  </acceptance_criteria>
  <done>
    Component interface extended with search props, imports added for Search and X icons.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement search icon and expandable input in header layout</name>
  <files>src/content/components/CacheStatusHeader.tsx</files>
  <behavior>
    - Test 1: Search icon (14x14px) renders on right side of header
    - Test 2: Input expands to 200px width when isSearchExpanded is true
    - Test 3: Input has placeholder "搜索提示词..."
    - Test 4: X icon (14x14px) appears when input is expanded
    - Test 5: Search icon is clickable (calls onSearchExpand)
    - Test 6: X icon click calls onSearchClose
  </behavior>
  <read_first>
    - src/content/components/CacheStatusHeader.tsx (current implementation after Task 1)
    - .planning/phases/08-search-collect-features/08-UI-SPEC.md (exact dimensions: 200px, 14x14px icons)
  </read_first>
  <action>
    Modify CacheStatusHeader.tsx return statement:

    1. Wrap header div content in a flex container with justifyContent: 'space-between'

    2. Left section: existing timestamp and status indicators (keep unchanged)

    3. Right section: Add search UI container with expand animation:

       ```typescript
       {/* Right section: Search UI (D-01, D-03) */}
       <div style={{ display: 'flex', alignItems: 'center', gap: '8px' }}>
         {/* Search icon toggle (D-02: 14x14px) */}
         <button
           onClick={onSearchExpand}
           aria-label="搜索提示词"
           style={{
             width: '24px',
             height: '24px',
             display: 'flex',
             alignItems: 'center',
             justifyContent: 'center',
             background: 'transparent',
             border: 'none',
             cursor: 'pointer',
             color: '#64748B',
           }}
         >
           <Search style={{ width: 14, height: 14 }} />
         </button>

         {/* Expandable input (D-03: 200px, 0.2s ease-out) */}
         <div style={{
           width: isSearchExpanded ? '200px' : '0px',
           overflow: 'hidden',
           transition: 'width 0.2s ease-out',
           transformOrigin: 'right',
         }}>
           <input
             value={searchQuery}
             onChange={(e) => onSearchChange?.(e.target.value)}
             placeholder="搜索提示词..."
             autoFocus={isSearchExpanded}
             aria-label="搜索提示词"
             style={{
               width: '200px',
               padding: '6px 10px',
               border: '1px solid #E5E5E5',
               borderRadius: '4px',
               fontSize: '12px',
               outline: 'none',
             }}
           />
         </div>

         {/* Close button (D-04: X icon, 14x14px) */}
         {isSearchExpanded && (
           <button
             onClick={onSearchClose}
             aria-label="关闭搜索"
             style={{
               width: '24px',
               height: '24px',
               display: 'flex',
               alignItems: 'center',
               justifyContent: 'center',
               background: 'transparent',
               border: 'none',
               cursor: 'pointer',
               color: '#171717',
             }}
           >
             <X style={{ width: 14, height: 14 }} />
           </button>
         )}
       </div>
       ```

    4. Update the parent div to use flex layout:
       ```typescript
       <div style={{
         padding: '4px 16px',
         fontSize: '10px',
         fontWeight: 400,
         color: isExpired ? '#A16207' : '#64748B',
         display: 'flex',
         alignItems: 'center',
         justifyContent: 'space-between',
         gap: '8px',
         borderBottom: '1px solid #E5E5E5',
       }}>
         {/* Left: Timestamp and status */}
         <div style={{ display: 'flex', alignItems: 'center', gap: '8px' }}>
           ...existing content...
         </div>
         {/* Right: Search UI */}
         ...new search UI...
       </div>
       ```
  </action>
  <verify>
    <automated>grep -n "width: isSearchExpanded" src/content/components/CacheStatusHeader.tsx && grep -n "搜索提示词" src/content/components/CacheStatusHeader.tsx</automated>
  </verify>
  <acceptance_criteria>
    - CacheStatusHeader.tsx contains `<Search style={{ width: 14, height: 14 }} />`
    - CacheStatusHeader.tsx contains `width: isSearchExpanded ? '200px' : '0px'`
    - CacheStatusHeader.tsx contains `placeholder="搜索提示词..."`
    - CacheStatusHeader.tsx contains `transition: 'width 0.2s ease-out'`
    - CacheStatusHeader.tsx contains `<X style={{ width: 14, height: 14 }} />`
    - CacheStatusHeader.tsx contains `onClick={onSearchClose}`
    - CacheStatusHeader.tsx contains `onClick={onSearchExpand}`
  </acceptance_criteria>
  <done>
    Search icon and expandable input UI implemented with correct dimensions (D-01 through D-04).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → Search query | Search text entered by user, needs sanitization for display |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | Search input | mitigate | React auto-escapes JSX content, no raw HTML rendering needed |
| T-08-02 | Spoofing | Search icon | accept | Icon click triggers controlled callback, no security impact |
</threat_model>

<verification>
- Manual: Click search icon in dropdown header, verify input expands from right
- Manual: Type in search input, verify placeholder disappears
- Manual: Click X icon, verify input collapses and clears
</verification>

<success_criteria>
- Search icon renders in CacheStatusHeader right side (D-01)
- Click expands input to 200px width (D-03)
- Input placeholder is "搜索提示词..." (D-03)
- X icon appears when expanded (D-04)
- Click X collapses and clears input (D-04)
</success_criteria>

<output>
After completion, create `.planning/phases/08-search-collect-features/08-01-SUMMARY.md`
</output>