---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/vision-api.ts
  - src/content/components/VisionModal.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Users see specific error messages for 404/403/500 errors instead of generic 'unknown error'"
    - "VisionModal placeholder shows correct endpoint guidance for both OpenAI and Anthropic"
  artifacts:
    - path: "src/lib/vision-api.ts"
      provides: "classifyApiError function with complete HTTP error coverage"
      min_lines: 10
      contains: "errorMessage.includes('404')"
    - path: "src/content/components/VisionModal.tsx"
      provides: "API config form with endpoint guidance"
      contains: "placeholder="
  key_links:
    - from: "src/lib/vision-api.ts"
      to: "VisionApiErrorPayload type"
      via: "return statement"
      pattern: "VisionApiErrorPayload"
---

<objective>
Fix Vision API error handling by adding missing HTTP status code handlers and improving endpoint placeholder guidance.

Purpose: Users receive specific, actionable error messages instead of generic "unknown error" when Vision API calls fail with 404/403/500 errors.
Output: Updated error classification and improved user guidance for API configuration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/debug/vision-api-network-error.md

<interfaces>
From src/shared/types.ts:
```typescript
export interface VisionApiErrorPayload {
  type: 'invalid_key' | 'rate_limit' | 'timeout' | 'network' | 'unsupported_image'
  message: string
  action: 'reconfigure' | 'retry' | 'close'
}
```

From src/lib/vision-api.ts:
```typescript
export function classifyApiError(error: unknown, retryCount = 0): VisionApiErrorPayload
```
</interfaces>
</context>

<tasks>

<task type="auto">
<name>Task 1: Add 404/403/500 error handlers to classifyApiError()</name>
<files>src/lib/vision-api.ts</files>
<action>
Update the `classifyApiError()` function (lines 235-291) to handle additional HTTP error codes:

1. Add handler for **404 (endpoint not found)**:
   - type: 'network'
   - message: 'API 端点不存在，请检查 Base URL 配置'
   - action: 'reconfigure'

2. Add handler for **403 (forbidden)**:
   - type: 'invalid_key'
   - message: 'API 访问被拒绝，请检查 API Key 权限'
   - action: 'reconfigure'

3. Add handler for **500/502/503 (server errors)**:
   - type: 'network'
   - message: 'API 服务暂时不可用，请稍后重试'
   - action: 'retry' (respecting MAX_RETRY_COUNT)

4. **Update the generic fallback** to log the actual error for debugging:
   - Add: `console.error('[Oh My Prompt] Unhandled API error:', error)`
   - Keep existing fallback but with logging

5. **Update VisionApiErrorPayload type** in `src/shared/types.ts` if needed to support new error type 'server_error' for 500-level errors (optional - 'network' works but 'server_error' is more semantic).

Insert new handlers BEFORE the generic fallback (line 285), after the existing 400 handler.
</action>
<verify>
<automated>npx tsc --noEmit</automated>
</verify>
<done>
- classifyApiError() handles 404, 403, 500/502/503 with specific messages
- Generic fallback logs actual error to console
- TypeScript compilation passes
</done>
</task>

<task type="auto">
<name>Task 2: Update VisionModal endpoint placeholder with provider-specific guidance</name>
<files>src/content/components/VisionModal.tsx</files>
<action>
Update the API Base URL placeholder (line 284) to provide clearer guidance for both OpenAI and Anthropic endpoints:

Change from:
```
placeholder="例如: https://api.openai.com/v1"
```

To:
```
placeholder="OpenAI: https://api.openai.com/v1/chat/completions | Anthropic: https://api.anthropic.com"
```

**Important distinction to note in comments:**
- OpenAI requires full path including `/v1/chat/completions`
- Anthropic uses base URL only (no `/messages` path) - the Messages API endpoint is handled automatically

Add a small hint below the input field if possible, or keep the placeholder concise since field is for base URL (the actual endpoint path construction happens in vision-api.ts based on provider detection).
</action>
<verify>
<automated>npx tsc --noEmit</automated>
</verify>
<done>
- VisionModal shows both OpenAI and Anthropic endpoint examples
- Placeholder reflects actual URL patterns users should enter
- TypeScript compilation passes
</done>
</task>

</tasks>

<verification>
1. TypeScript check passes: `npx tsc --noEmit`
2. Manual verification: Trigger various HTTP errors and confirm specific messages appear
</verification>

<success_criteria>
- classifyApiError() returns specific error payloads for 404, 403, 500, 502, 503
- Generic fallback logs actual error for debugging
- VisionModal placeholder shows correct examples for both providers
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/quick/260429-0f9-fix-vision-api-error-handling-add-handle/260429-0f9-SUMMARY.md`
</output>